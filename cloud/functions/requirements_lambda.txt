# AWS Lambda Model Training Requirements
# Optimized for AWS Lambda environment

# Core AWS and Cloud
boto3>=1.28.0
botocore>=1.31.0

# Core ML and Data Science (lightweight versions for Lambda)
numpy>=1.24.0,<1.25.0  # Pinned for stability in Lambda
pandas>=2.0.0,<2.1.0   # Optimized for Lambda memory constraints
scikit-learn>=1.3.0,<1.4.0

# Deep Learning (CPU-optimized for Lambda)
torch>=2.0.0,<2.1.0 --index-url https://download.pytorch.org/whl/cpu

# Financial Data
yfinance>=0.2.18
requests>=2.31.0

# Utilities
retrying>=1.3.4
python-dotenv>=1.0.0

# Logging and Monitoring
structlog>=23.1.0

# Data Processing
pytz>=2023.3